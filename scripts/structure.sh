#!/bin/sh

SRC_PATH="../src/main/java/com/example/demo"

mkdir "${SRC_PATH}/repository"
mkdir "${SRC_PATH}/service"
mkdir "${SRC_PATH}/controller"
mkdir "${SRC_PATH}/model"
mkdir "${SRC_PATH}/config"
mkdir "${SRC_PATH}/security"

echo "Create spring base src structure"